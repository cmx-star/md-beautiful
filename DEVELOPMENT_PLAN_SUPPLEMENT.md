# Markdown Beautiful 补充开发计划

> 基线日期：2026-08-25  
> 适用范围：当前仓库中的 Tauri 2 + Vue 3 原型  
> 文档性质：后续开发路线与验收补充，不代表候选技术均已安装或实现

## 1. 文档目的

本计划在现有原型和原始技术方案基础上，补齐以下内容：

- 以当前代码为事实基线，区分“已有界面原型”“已有底层接口”和“尚未实现的产品能力”。
- 将产品目标收敛为“Bear 式写作体验 + Obsidian 式本地文件模型 + 轻量 Notion 属性能力”。
- 优先解决数据安全、文件主权、预览安全和同步冲突，再扩展双链、搜索、公式与插件能力。
- 为每个阶段给出可核验的交付物、检查项和退出条件。

## 2. 当前仓库基线

### 2.1 已确认存在

| 领域 | 当前实现 | 结论 |
| --- | --- | --- |
| 桌面宿主 | Tauri 2、Rust | 保留 |
| 前端 | Vue 3.5、Composition API、TypeScript、Vite 5 | 保留 |
| 状态管理 | Pinia 3、持久化插件 | 保留，但笔记内容不应继续以浏览器存储为最终数据源 |
| 编辑器 | CodeMirror 6 Markdown 模式 | MVP 继续使用，不迁移到 ProseMirror/Milkdown |
| Markdown 预览 | `marked` 生成 HTML，前端直接写入 DOM | 可用原型，必须先补安全清洗 |
| 公式 | 本地打包 MathJax 3 浏览器运行时，检测到公式后按需加载并输出 SVG | 已离线可用，仍在 WebView 主线程排版，不是 QuickJS 或 Worker 方案 |
| 本地文件接口 | Rust 已有 Vault 与单 Markdown 文件的打开、读写、创建、删除命令 | Vault 与原生选择器授权文件已使用原子写入；完整文件监听和冲突中心仍未实现 |
| 远端接口 | Rust 已有 GitLab 与 WebDAV 请求雏形 | 仅作为当前代码事实记录；后续同步规划按 Git 托管仓库同步与 WebDAV 协议同步两类设计，不沿用 GitLab 作为目标主线 |
| UI | 侧栏、工具栏、编辑区、预览区、命令面板、同步面板、主题 | 可作为后续迭代的界面骨架 |
| 样式 | Tailwind CSS 3.4 | 当前不是原方案所写的 Tailwind CSS 4 |

### 2.2 需要纠正的状态描述

1. 当前笔记由 Pinia 持久化到 `localStorage`，尚未做到“本地 `.md` 文件是唯一事实来源”。
2. 同步面板的 `startSync()` 目前仅模拟等待并写入“同步完成”日志，没有执行扫描、拉取、上传、删除或冲突合并。
3. Rust 侧 `process_mathjax` 没有嵌入 QuickJS，也不执行 MathJax 渲染；公式由前端按需加载本地 MathJax 浏览器资源并输出 SVG。
4. Markdown 预览已经接入白名单清洗和严格 CSP；仍需持续用恶意 Markdown 语料验证 URL、属性与嵌套 HTML 边界。
5. Vault 文件命令已限制在授权根目录内；单文件仅允许保存原生选择器在本次运行中授权的 `.md` / `.markdown` 路径，并在写入前检查文件指纹。完整的文件监听、Diff 和冲突中心仍待实现。
6. 仓库没有项目级测试文件和 CI 配置，`README.md` 仍描述 Electron、React、Monaco、Zustand，与实际代码不一致。

因此，现阶段应定义为“可交互桌面原型”，而不是已经完成本地优先与同步能力的 MVP。

## 3. 产品与架构约束

### 3.1 必须保持

- Markdown 文件是正文的唯一事实来源；SQLite、搜索索引和缩略图均可删除后重建。
- 不自动整篇重排 Markdown，避免制造无意义 Git Diff。
- 不静默覆盖外部修改、远端新版本或无法自动合并的内容。
- 密钥、PAT、密码不得写入 Markdown、普通配置文件、日志或前端持久化存储。
- MVP 保留源码编辑模型，不同时引入块编辑器、多人实时协作、插件市场和任意数据库块。
- 同一个 Vault 同时只允许一种应用内同步方式，系统云盘目录与应用内同步的组合必须显式警告。

### 3.2 新增需求覆盖矩阵

| 需求 | 是否已在计划内 | 补充说明 |
| --- | --- | --- |
| Windows / Linux 等主流系统跨平台支持 | 已覆盖 | Phase 6 已包含 Windows、macOS、Linux 构建、安装、升级和发布验收 |
| LaTeX 公式、脚注、尾注 | 部分覆盖，需补强 | Phase 2 已覆盖脚注和数学公式；补充尾注作为 Markdown 方言与导出兼容项 |
| 本地 MathJax，不依赖 CDN | 已覆盖 | Phase 0/5 已要求 MathJax 本地打包、离线启动和 Worker 压测 |
| 用户可配置 Markdown 语法高亮 | 需显式加入 | Phase 2 增加语法高亮主题、Token 样式和用户配置项 |
| 多格式导出 | 需显式加入 | 增加 Export Service，优先评估 Pandoc CLI 或 Pandoc WebAssembly，用于 HTML、DOCX、ODT、LaTeX、PDF、reStructuredText、MediaWiki、EPUB、TXT 等格式 |
| 用户指定 CSS 控制显示式样 | 需显式加入 | 阅读预览和导出链路都要支持用户 CSS；必须做路径授权、隔离加载和失败回退 |
| 简洁友好、突出编辑内容的界面 | 已覆盖，需参考落地 | 以 Markdown Preview 的阅读布局、文档大纲、搜索、工具栏定制作为体验参考，但不复制其 macOS-only 技术路线 |
| 系统、完备的快捷键 | 部分覆盖，需补强 | 增加快捷键注册中心、冲突检测、用户自定义和跨平台按键文案 |

### 3.3 参考产品吸收边界

- `pluk-inc/markdown-preview` 可参考其干净阅读视图、文档大纲、工具栏搜索、编辑切换、缩放、Open With、Quick Look 等产品细节；本项目仍以 Tauri + Vue 跨平台桌面应用为主，不采用其 macOS AppKit/WKWebView 单平台路线。
- `pandoc.app` 可参考“本地转换、不上传文件”的导出体验；本项目导出能力应抽象为 Export Service，底层可在 Pandoc CLI、Pandoc WebAssembly 或系统打印/PDF 之间按平台能力选择。
- 导出与预览必须共享 Markdown 方言、公式策略、资源路径解析和用户 CSS 规则，避免“预览一种效果，导出另一种效果”。

### 3.4 技术路线调整

| 主题 | 原始设想 | 补充计划 |
| --- | --- | --- |
| 编辑器 | ProseMirror/Milkdown 块编辑 | MVP 继续 CodeMirror 6；用 Decoration、命令和预览改善写作体验 |
| 公式 | Rust + QuickJS + MathJax 主链路 | 先验证本地打包的 MathJax + Web Worker；QuickJS 留作二期受限脚本沙箱 |
| 数据 | 内存 JSON/浏览器存储 | Vault 中标准 Markdown；应用数据目录存可重建索引与设备状态 |
| 搜索 | 前端 Fuse.js | 小规模先复用，达到数据量阈值后再评估 Rust 全文索引 |
| 同步 | 当前代码残留 GitLab/WebDAV 原型 | 重构为两类同步：Git 托管仓库同步（GitHub/Gitee）与 WebDAV 协议同步，并加入基线、条件写入、冲突中心和凭据库 |
| 安全 | 依赖 WebView/CSP | 本地依赖、严格 CSP、HTML 清洗、Vault 路径边界和最小 Tauri 权限共同防护 |

### 3.5 页面、主题与图标优化方案

本轮界面以 Bear 的桌面信息层级为产品参考，以 `imageslr/mweb-themes` 的 Bear 主题参数为正文排版参考。只吸收布局、密度、颜色关系和阅读节奏，不复制 Bear 商标、熊头图形、订阅标识或尚未实现的云服务能力。

**桌面外壳**：

- 采用“深石墨导航 + 白色笔记列表 + 纸张色编辑/预览”的三栏结构，让导航、管理和写作区域形成稳定层级。
- macOS 使用系统窗口按钮和 Overlay 标题栏，隐藏系统居中标题；顶部采用 30 px 系统按钮/拖拽区加 50 px 应用工具栏的双层结构，不绘制第二套窗口按钮。Windows、Linux 不增加该 30 px 系统标题栏层。
- 主强调色使用珊瑚红 `#e25555`，链接使用蓝色，高亮与完成状态使用绿色，避免界面被单一色相支配。
- 常用动作统一使用 Lucide 线性图标，并为不熟悉的图标提供名称；不再混用 Emoji、文本符号和手绘 SVG。
- 默认侧栏宽度为 440 px，其中导航 176 px、笔记列表 264 px；视口宽度不超过 1100 px 时，侧栏收窄为 330 px，其中图标导航 64 px、笔记列表 266 px。
- 应用窗口保持最小 900 × 600 px；该尺寸下编辑区与预览区仍可并排操作，不产生水平页面溢出。

**Markdown 正文**：

- 正文字号与行高基线为 `16px / 1.6`，阅读列最大宽度约 `46rem`。
- 正文字体优先 Avenir Next 与系统字体；代码字体优先 Menlo、SFMono-Regular 等平台等宽字体。
- 标题、引用、列表、任务项、行内代码、代码块和表格分别设置层级与间距，不直接复制旧 Sass 主题。
- Bear 外壳和 Markdown 正文主题保持解耦，后续用户主题仅替换阅读变量与正文规则，不应改变桌面导航结构。

**应用图标**：

- 原创图形采用“纸页 + 珊瑚红编辑光标”，底色为深石墨色，与应用外壳形成统一识别。
- 保留 `src-tauri/icons/app-icon.svg` 作为可编辑源文件，通过 Tauri CLI 生成 PNG、ICNS、ICO 及移动端资源。
- 不使用 Bear 熊头、Bear 字标或容易造成来源混淆的轮廓。

**当前验证范围与已知风险**：

- `bundle.targets` 保持 `"all"`，不把产品发布范围收窄为 macOS；当前开发机器只实际构建和验收 macOS DMG。
- 本机 DMG 使用 `npm run tauri:build -- --bundles dmg` 定向生成。Windows、Linux 安装包仍需在对应平台或 CI 构建矩阵中验证。
- MathJax 已改为本地按需浏览器资源，并在严格 CSP 下完成行内与块级公式 SVG 回归；大文档中的主线程排版性能仍需评估 Worker 或缓存方案。
- 当前生产主包超过 500 kB 警告阈值，后续应按编辑器、预览与公式运行时拆分异步模块，并以启动时间和交互延迟验证优化收益。

### 3.6 当前迭代已落地能力

- 工具栏和命令面板均提供“打开 Markdown 文件”，快捷键为 `⌘O`；“打开 Vault”调整为 `⌘⇧O`。
- 单文件通过系统原生选择器授权，只接受 `.md` 与 `.markdown`。打开后保留规范化绝对路径，编辑内容保存回原文件。
- 保存前比较毫秒级修改时间与文件大小；检测到外部程序修改时拒绝覆盖并显示保存失败。写入使用同目录临时文件、刷盘和原子替换。
- 工具栏与预览中的“字数”统一为 Markdown 渲染后可见字符数：排除空白和常见公式分隔符，中文、英文和数字均按可见字符计数。持久化字段仍沿用 `wordCount`，避免破坏已有数据结构。
- 笔记标题从首个 Markdown 文本块提取，并在编辑时同步更新列表、工具栏和预览标题。
- Finder 双击文件、系统“打开方式”和 Tauri 单实例文件事件关联不在本轮范围；当前入口是应用内工具栏、命令面板与 `⌘O`。

## 4. 目标架构

```text
Vue 3 UI
├── Workspace：Vault、文件树、标签、搜索
├── Editor：CodeMirror、命令、附件、草稿恢复
├── Reader：安全预览、公式、用户 CSS、属性、反向链接
├── Export：HTML、DOCX、ODT、LaTeX、PDF、RST、MediaWiki、EPUB、TXT
└── Sync Center：状态、Diff、冲突与凭据入口
             │ Tauri Commands / Events
Rust Core
├── Workspace Service：路径边界、文件监听、原子保存
├── Markdown Service：Frontmatter、标签、Wiki Link、引用提取
├── Index Service：元数据、反向链接、全文索引
├── Export Service：Pandoc/打印管线、资源收集、模板与 CSS
├── Sync Service：Git 托管仓库同步、WebDAV 协议同步、系统同步目录协作
├── Credential Service：系统凭据库
└── Diagnostics：结构化日志、崩溃恢复、性能指标
```

建议的 Vault 结构：

```text
vault/
├── notes/**/*.md
├── assets/**
└── .mdapp/vault.json
```

索引数据库、搜索数据、预览缓存和设备同步游标放在系统应用数据目录，不提交到 Vault。`.mdapp/vault.json` 只保存不敏感、可迁移的 Vault 级设置。

## 5. 分阶段开发计划

### Phase 0：基线校准与安全止血

**目标**：让文档、构建和当前真实能力一致，先关闭明显的数据与渲染风险。

**主要交付**：

- 修正 `README.md` 的技术栈、目录结构、里程碑和“同步已完成”等失真描述。
- 建立最小测试骨架和 CI：前端类型检查、构建，Rust 格式检查、静态检查和单元测试。
- 为 Markdown 预览增加白名单清洗，禁止脚本、事件属性、危险 URL 和远程 iframe。
- 将 MathJax 与字体等运行时资源改为随应用本地打包，收紧 CSP。
- 形成 IPC 错误结构、日志脱敏和用户可见错误提示规范。

**退出条件**：

- `npm run typecheck`、`npm run build` 通过。
- Rust 侧 `cargo fmt --check`、`cargo check` 通过。
- 恶意 Markdown 回归样例不能执行脚本、事件处理器或 `javascript:` URL。
- 应用离线启动时编辑器和公式基础能力不依赖公共 CDN。

### Phase 1：本地优先与数据安全

**目标**：完成真正的 Vault 打开、读取、编辑、保存、监听和恢复闭环。

**主要交付**：

- Vault 选择、最近打开列表、文件树和 Markdown 文件加载。
- 所有文件操作限制在已授权 Vault 内，校验规范化路径和符号链接边界。
- 自动保存采用“同目录临时文件 + 刷盘 + 原子替换”；保存前记录已读取版本指纹。
- 外部修改监听：未编辑时刷新，存在本地未保存修改时进入 Diff/冲突流程。
- 草稿恢复：异常退出后可恢复未落盘内容，不自动覆盖源文件。
- 附件导入、相对路径生成、重名处理和孤儿附件策略。
- 将笔记正文从 `localStorage` 迁出；浏览器存储只保留非敏感 UI 偏好。

**退出条件**：

- 新建、打开、编辑、重启、恢复、重命名和删除均作用于真实 Markdown 文件。
- 强制结束应用后，源文件保持完整，草稿可恢复。
- 应用外修改文件时不会静默丢失任一版本。
- 尝试通过 `..`、绝对路径或符号链接访问 Vault 外文件时被拒绝并记录脱敏错误。

### Phase 2：写作体验与 Markdown 兼容

**目标**：在不改变纯文本事实源的前提下，形成接近 Bear 的专注写作体验。

**主要交付**：

- 明确 Markdown 方言：CommonMark、GFM 表格、任务列表、Frontmatter、脚注、尾注、Wiki Link 和数学公式。
- CodeMirror 命令：标题、列表、引用、代码、链接、表格、任务和公式插入。
- 用户可配置 Markdown 语法高亮：主题选择、标题/列表/引用/代码/链接/公式等 Token 样式、浅色/深色独立配置和重置默认值。
- 渐进隐藏 Markdown 标记，只隐藏非活动语法，不破坏光标、选择、输入法和撤销栈。
- 源码、实时预览、阅读三种模式；滚动同步作为可关闭能力。
- Frontmatter 属性面板与文本双向同步，未知字段必须原样保留。
- 快捷键注册中心：覆盖新建、打开、保存、搜索、命令面板、格式化命令、模式切换、导出、同步、侧栏/预览切换，并支持冲突检测和用户自定义。
- 大文档采用增量解析、可见区装饰和公式缓存，避免每次输入全量重排。

**退出条件**：

- 代表性 Markdown 语料可无损打开、编辑、保存和再次解析。
- 中文输入法、撤销/重做、复制粘贴、多光标和大纲跳转通过人工回归。
- 用户修改语法高亮配置和快捷键后，立即生效；重启后配置仍保留；冲突快捷键必须阻止保存或给出明确提示。
- 未知 Frontmatter 字段、HTML 片段和自定义语法不会被无提示删除或改写。
- 5 MB 文档连续输入时无明显主线程长时间阻塞；正式阈值以性能基线测试确定。

### Phase 3：知识管理与索引

**目标**：补齐标签、双链、反向链接、属性查询和可重建搜索索引。

**主要交付**：

- 解析 `[[Wiki Link]]`、标题锚点、块引用、标签、别名和 Frontmatter。
- 为链接目标定义稳定解析规则：显式 ID 优先，其次相对路径，再按别名/标题匹配；歧义必须提示用户。
- 增量维护文档元数据、出链、反链和未解析链接。
- 支持按标题、正文、标签和属性搜索；启动时后台增量索引，不阻塞编辑。
- 关系图只作为索引的视图，不另建不可重建的数据源。

**退出条件**：

- 删除索引后可仅从 Vault 完整重建标签、链接和搜索数据。
- 重命名文件前展示受影响链接，批量更新必须可预览、可取消并产生备份。
- 10,000 篇普通笔记下，索引在后台推进且 UI 可继续编辑。
- 已建立索引时，常规关键词查询 P95 目标小于 150 ms；测试机器和数据集必须随结果记录。

### Phase 4：可靠同步与冲突中心

**目标**：把现有远端请求接口升级为不会静默丢数据的同步系统。

**统一同步流程**：

```text
读取本地与远端状态
→ 对比上次共同基线
→ 生成操作计划
→ 用户确认破坏性操作
→ 拉取/三方合并
→ 上传条件写入
→ 更新基线与游标
```

**主要交付**：

- 定义统一 SyncProvider 契约：扫描、拉取、上传、删除、冲突检测、取消和状态事件。
- 同步类型分为两类：Git 托管仓库同步与 WebDAV 协议同步；GitHub/Gitee 是 Git 托管仓库同步下的首期平台适配。
- Git 托管仓库同步优先验证“平台仓库文件 API”和“本地 Git 仓库 + push/pull”两条路线，完成可验证技术试验后再确定默认实现。
- GitHub/Gitee 均禁止自动强推；删除、覆盖、分支切换和冲突解决必须进入可审计操作计划。
- WebDAV 协议同步作为另一类同步能力并行规划，重点验证 ETag、`If-Match`、临时资源/MOVE、服务端兼容性和冲突保留策略。
- 文本冲突执行基于共同基线的三方合并；失败时保留双方文件并打开 Diff UI。
- 二进制附件不自动合并，不以“最后写入者胜出”静默覆盖。
- PAT、密码等通过系统 Keychain/Credential Manager/Secret Service 保存。
- 同步日志不得包含 Token、密码、Authorization 头或完整私密正文。

**退出条件**：

- 断网、超时、重复请求、远端删除、双方修改和凭据失效均有自动化或可重复集成测试。
- 任一同步失败后可安全重试，不产生重复文件、错误删除或虚假“同步完成”。
- 冲突测试证明本地和远端版本均可找回。
- 日志与配置扫描未发现明文凭据。

### Phase 5：公式性能、导出与受限扩展

**目标**：先解决公式性能和多格式导出，再决定是否引入 QuickJS。

**公式技术试验**：

- 对比 MathJax 本地主线程、MathJax Web Worker、按可见区懒渲染三种组合。
- 测试 100、500、1,000 个混合公式的首次排版、滚动、增量编辑和内存占用。
- 使用公式源码、渲染配置和主题组成缓存键，避免重复排版。
- Worker 不满足兼容性或性能指标时，再评估 Rust/QuickJS 离屏渲染成本。

**导出服务**：

- 建立 Export Service，前端只提交文档、目标格式、资源清单、用户 CSS 和导出选项，不直接拼接各格式文件。
- 优先评估 Pandoc CLI 或 Pandoc WebAssembly，覆盖 HTML、Microsoft Word DOCX、OpenOffice/LibreOffice ODT、LaTeX、PDF、reStructuredText、MediaWiki markup、EPUB 和 plain TXT。
- PDF 导出允许两条路径：Pandoc + LaTeX 引擎，或 WebView 打印为 PDF；需要按公式、脚注、尾注、代码高亮和用户 CSS 效果实测后确定默认方案。
- 导出资源收集必须支持相对图片、附件、用户 CSS、字体和公式输出；缺失资源时给出报告，不静默生成残缺文件。
- 导出配置应支持当前文档、选中文件、多文件合并和模板参数；MVP 可先实现当前文档导出。

**QuickJS 使用边界**：

- 二期仅用于模板计算或用户脚本沙箱，不直接获得文件、网络、Shell 或 Tauri IPC 权限。
- 必须具备内存、栈、执行时间、中断和输出大小限制。
- 插件 API 未冻结前不建设插件市场，也不承诺第三方兼容契约。

**退出条件**：

- 公式压力语料下编辑输入保持可用，滚动无持续长任务；指标以 Phase 0 建立的性能记录方式判定。
- 公式渲染失败只影响对应公式块，并提供源码和错误信息，不破坏文档正文。
- 代表性文档可导出为 HTML、DOCX、ODT、LaTeX、PDF、RST、MediaWiki、EPUB 和 TXT；导出结果保留标题层级、公式、脚注、尾注、代码块、图片和用户 CSS 中的核心样式。
- 导出失败时不会覆盖已有目标文件；错误报告包含失败阶段、缺失资源和可重试建议。
- 如启用 QuickJS，越权、死循环、内存膨胀和超大输出测试均被限制机制拦截。

### Phase 6：发布与质量验收

**目标**：形成可安装、可升级、可诊断、可回退的三平台发行版本。

当前阶段仅在 macOS 开发机上生成和验证 DMG 测试包，不代表发行物只保留 DMG，也不代表 Windows、Linux 安装包已经通过验证。

**主要交付**：

- Windows、macOS、Linux 构建矩阵，完成签名、公证或平台要求的安装验证。
- 自动更新必须校验签名；升级失败不得损坏 Vault 或应用配置。
- 崩溃恢复、日志导出、版本信息和隐私说明。
- 安装、首次启动、打开 Vault、编辑、搜索、导出、同步、升级、卸载的端到端回归。
- 无障碍、键盘操作、缩放、高对比度和主要分辨率检查。

**退出条件**：

- 三个平台完成安装、卸载、升级和核心编辑流程验证。
- 删除应用及缓存后，用户仍能用任意文本编辑器读取全部 Markdown 与附件。
- 发布包不包含测试密钥、开发地址、调试权限或远程运行时依赖。
- 导出功能在三平台至少完成 HTML、DOCX、PDF、EPUB 和 TXT 冒烟测试；ODT、LaTeX、RST、MediaWiki 如因底层工具缺失不能随包提供，必须在发行说明中明确限制。

- 三个平台完成安装、卸载、升级和核心编辑流程验证。
- 删除应用及缓存后，用户仍能用任意文本编辑器读取全部 Markdown 与附件。
- 发布包不包含测试密钥、开发地址、调试权限或远程运行时依赖。

## 6. 下一迭代建议（1～2 周）

下一迭代只处理基础可信度，不并行铺开知识图谱和完整同步：

1. 修正文档与实际技术栈，建立当前构建基线。
2. 给预览链路补 HTML 清洗和恶意 Markdown 回归测试。
3. 将 MathJax 从 CDN 迁为本地资源，并收紧 CSP。
4. 设计并实现 Vault 打开、受限读取和原子保存的最小纵向闭环。
5. 增加外部修改检测的可重复测试，确定版本指纹和冲突提示语义。
6. 把同步 UI 的“模拟完成”改为明确的未配置/未实现状态，避免误导用户。

该迭代完成后再进入 Frontmatter、双链和全文索引开发。

## 7. 候选依赖与审批点

以下仅为候选项，实施前必须结合验证结果说明修改哪个包、原因、风险、替代方案和安装命令，并单独取得依赖变更批准。

| 能力 | 候选方向 | 不增加依赖的替代方案 | 主要风险 |
| --- | --- | --- | --- |
| HTML 清洗 | 前端成熟 sanitizer，或 Rust 白名单清洗 | 自建有限规则 | 自建规则容易遗漏解析器边界和危险 URL |
| Markdown AST | unified/remark 生态 | 继续使用 `marked` 并自建扩展 | 双向语法、位置信息和插件兼容成本 |
| 文件监听 | Rust `notify` | 定时扫描 | 平台事件差异、重复事件、重命名识别 |
| 元数据 | SQLite 驱动 | 内存索引 + JSON 缓存 | 迁移、并发、损坏恢复与打包体积 |
| 全文搜索 | Tantivy/其他 Rust 索引 | Fuse.js 或 SQLite FTS | 中文分词、索引体积、首次构建耗时 |
| 凭据 | 系统凭据库封装 | 每次启动手动输入 | 各平台后端差异与自动化测试困难 |
| Git 托管仓库同步 | `git2`/libgit2 | GitHub/Gitee 仓库文件 API | 凭据、合并语义、原生库打包复杂度 |
| WebDAV 协议同步 | 标准 WebDAV HTTP 客户端 | 用户选择系统云盘同步目录 | 服务端兼容性、ETag 支持差异、锁和条件写入语义 |
| 多格式导出 | Pandoc CLI 或 Pandoc WebAssembly | HTML 使用安全预览渲染、PDF 使用 WebView 打印、TXT 直接写出 | 打包体积、外部二进制授权、LaTeX/PDF 引擎依赖、跨平台路径与字体差异 |
| 公式 | 本地 MathJax + Worker | 当前主线程渲染 | Worker 环境兼容、字体与输出缓存 |
| 脚本沙箱 | `rquickjs` | 不开放用户脚本 | 资源限制、宿主 API 暴露和维护成本 |

## 8. 测试与验收矩阵

| 范围 | 最小检查 | 扩大检查触发条件 |
| --- | --- | --- |
| Vue 组件/状态 | 类型检查、相关单元测试、定向交互测试 | 修改 App 壳、公共 Store 或编辑器数据流时运行全量前端测试和构建 |
| Rust 文件服务 | 单元测试、临时目录集成测试、`cargo check` | 修改路径边界、保存、删除或迁移时增加崩溃与数据完整性测试 |
| Markdown | 固定语料快照、恶意输入、往返测试 | 修改解析器、序列化器或 Frontmatter 时运行全语料回归 |
| 索引 | 增量更新、删除重建、性能样本 | 修改 Schema 或链接解析规则时运行迁移与全量重建测试 |
| 导出 | 单文档多格式冒烟、资源缺失报告、目标文件不覆盖检查 | 修改 Pandoc 参数、模板、用户 CSS、公式或附件解析时运行导出格式矩阵 |
| 同步 | 假服务/测试仓库、断网、并发修改、幂等重试 | 修改删除、覆盖、凭据或合并逻辑时运行端到端冲突矩阵 |
| 发布 | 构建、安装、启动、升级冒烟 | 修改 Tauri 权限、签名、更新器或原生依赖时运行三平台验收 |

每个“完成”声明至少需要对应：实现证据、受影响范围检查、失败路径检查、无关文件未被夹带。只通过静态检查时，不得外推为桌面运行和用户可见行为已通过。

## 9. 里程碑与粗略工期

| 里程碑 | 建议周期 | 可审核结果 |
| --- | --- | --- |
| M0 基线与安全 | 1～2 周 | 文档准确、构建基线、预览安全、本地运行时资源 |
| M1 本地优先 MVP | 3～4 周 | Vault、原子保存、监听、恢复、附件 |
| M2 写作与知识管理 | 4～6 周 | Markdown 体验、属性、双链、搜索、索引 |
| M3 可靠同步 | 3～4 周 | Git 托管仓库同步（GitHub/Gitee）、WebDAV 协议同步、凭据、冲突中心 |
| M4 发布候选 | 2～3 周 | 三平台安装、升级、导出、性能与稳定性验收 |

按 2 名开发加兼职设计/测试估算，达到可公开测试的桌面 MVP 仍建议预留约 13～19 周。当前 UI 原型能减少界面搭建时间，但数据层、安全、同步和发布链路仍需按正式产品重新验收。单人开发可按 5～7 个月规划，并优先砍掉关系图、QuickJS 和复杂属性视图。

## 10. 关键风险与决策门槛

| 风险 | 触发信号 | 处理原则 |
| --- | --- | --- |
| Markdown 往返损坏 | 未知语法或 Frontmatter 被改写 | 停止自动写回，先扩充语料和保真策略 |
| 外部修改丢失 | 保存版本与读取版本不一致 | 进入冲突流程，禁止覆盖 |
| 同步环路 | 同一 Vault 同时启用多种同步 | 阻止配置或要求用户明确切换 |
| 公式架构过早复杂化 | Worker 尚未压测就引入 QuickJS | 先完成基准，再决定 Rust 侧渲染 |
| 索引方案过重 | 小规模数据也需要复杂原生依赖 | 先定义阈值和数据集，按证据升级 |
| 凭据泄露 | Token 出现在 Store、日志或配置 | 阻断发布，迁移系统凭据库并轮换测试凭据 |
| README 与实现再次漂移 | 里程碑状态没有验证链接或检查结果 | 状态只使用“原型/进行中/已验证”并附证据 |

## 11. 暂不纳入 MVP

- Notion 式任意块数据库和多人实时协作。
- ProseMirror/Milkdown 主编辑器迁移。
- 插件市场和不受限第三方脚本。
- 同时运行多个应用内同步提供方。
- 各云盘厂商专有 API；MVP 只支持用户选择其系统同步目录。
- 移动端、Web 端和端到端加密同步。
- 在缺少性能证据前引入 QuickJS 作为公式主链路。

## 12. 完成定义

一个阶段只有在以下条件全部满足时才能标记完成：

- 阶段列出的用户行为已经实现，失败路径有明确处理。
- 相关类型检查、构建、单元/集成测试和必要的桌面人工回归通过。
- 数据格式、IPC 契约、权限、文档和迁移说明在授权范围内同步更新。
- 未静默丢失文件、覆盖冲突、泄露凭据或放宽安全权限。
- 没有夹带与阶段无关的依赖、重构或生成文件变更。
- 未运行的检查、原因和残余风险在交付结果中明确列出。
